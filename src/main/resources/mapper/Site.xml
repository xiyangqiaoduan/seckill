<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yangcb.seckill.dao.site.SiteDao">
	<!-- 获取站点信息 -->
	<select id="getSite" resultType="Site">

		SELECT
			site_id,site_name,site_url,site_logo,site_keyword,site_description,site_copyright,site_style,site_mobile_style,site_mobile_State
		FROM
			site limit 1

	</select>

	<update id="updateSite"  parameterType="Site">
		UPDATE site SET 
			site_name=#{siteName},
			site_url=#{siteUrl},
			site_logo=#{siteLogo},
			site_keyword=#{siteKeyword},
			site_description=#{siteDescription},
			site_copyright=#{siteCopyright},
			site_style=#{siteStyle},
			site_mobile_style=#{siteMobileStyle},
			site_mobile_state=#{siteMobileState}
		where 
			site_id=#{siteId}
	</update>


</mapper>